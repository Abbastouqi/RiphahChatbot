# --- HTTP / crawling ---
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
tenacity>=8.2.0
pymupdf>=1.24.0          # PDF text extraction (prospectus, calendars)

# --- knowledge base ---
numpy>=1.26.0            # cosine similarity over the vector store

# --- models ---
openai>=1.51.0           # embeddings + Realtime voice
anthropic>=0.69.0        # structured extraction + text-mode agent

# --- backend ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.10.0
